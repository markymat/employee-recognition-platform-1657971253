<!DOCTYPE html>
<html>
  <head>
    <title>Employee Recognition Platform</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body>
  <h4><%= link_to 'Company', root_path %></h4>
   <p class="notice"><%= notice %></p>
   <p class="alert"><%= alert %></p>
    <%= yield %>
    <nav>
      <h4>Navigation</h4>
      <ul>
        <% if !employee_signed_in? %>>
          <li><%= link_to 'Sign_in', new_employee_session_path %></li>
          <li><%= link_to 'Sign_up', new_employee_registration_path  %></li>
            <% else %>
          <li><%= link_to 'Sign_out', destroy_employee_session_path, method: :delete %></li>
          <% end %>
      </ul>  
    </nav>
    
  </body>
</html>
